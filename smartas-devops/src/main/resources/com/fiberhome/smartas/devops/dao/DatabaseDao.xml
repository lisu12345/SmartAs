<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fiberhome.smartas.devops.dao.DatabaseDao">

	<!-- <cache /> -->

	<select id="getTableCountAll" resultType="int" databaseId="mysql">
		SELECT
		count(1)
		FROM information_schema.tables t 
		WHERE t.table_schema=#{db} AND t.table_name LIKE CONCAT(#{prefix},'%')
	</select>

	<select id="selectTable" resultType="com.fiberhome.smartas.devops.Table" pageable="true"  databaseId="mysql">
		SELECT t.table_schema as "schema",t.table_name as "name",t.table_comment as "comment" 
		FROM information_schema.tables t 
		WHERE t.table_schema=#{db} AND t.table_name LIKE CONCAT(#{prefix},'%')
	</select>
	
	<select id="getTableCountAll" resultType="int" databaseId="oracle">
		SELECT
		count(1)
		FROM user_tab_comments t 
		WHERE t.table_type='TABLE' AND t.table_name LIKE CONCAT(#{prefix},'%')
	</select>
	
	<select id="selectTable" resultType="com.fiberhome.smartas.devops.Table" pageable="true" databaseId="oracle">
		SELECT t.table_name as "name",t.comments as "comment"
		FROM user_tab_comments t 
		WHERE t.table_type='TABLE' AND t.table_name LIKE CONCAT(#{prefix},'%')
	</select>

	<!-- //////////////////////////////////////////////////////////// -->
 
</mapper>

