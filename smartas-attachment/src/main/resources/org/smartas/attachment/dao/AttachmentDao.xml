<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.smartas.attachment.dao.AttachmentDao">

	<!-- <cache /> -->

	<!-- ///////////////////////////基础接口定义///////////////////////////////// -->

	<select id="getById" resultType="org.smartas.attachment.Attachment">
		SELECT
		T.*
		FROM tpl_file_t T
		WHERE T.ID = #{id}
	</select>

	<select id="getCount" resultType="int">
		SELECT
		count(1)
		FROM tpl_file_t T
	</select>

	<select id="selectAll" resultType="org.smartas.attachment.Attachment">
		SELECT
		T.*
		FROM tpl_file_t T
		<query open="where (" close=")" />
	</select>

	<select id="select" resultType="org.smartas.attachment.Attachment" pageable = "true">
		SELECT T.* FROM tpl_file_t T
		<query open="where (" close=")" />
	</select>


	<insert id="insert" parameterType="org.smartas.attachment.Attachment">
		INSERT INTO tpl_file_t
		(batch_no,file_path, file_name, file_uid , revision, create_user_id, create_date , last_update_user_id , last_update_date)
		VALUES
		(#{batchNo,jdbcType=VARCHAR},
		#{filePath,jdbcType=VARCHAR},
		#{fileName,jdbcType=VARCHAR},
		#{fileUid,jdbcType=VARCHAR},
		#{revision,jdbcType=NUMERIC},
		#{createUserId,jdbcType=VARCHAR},
		NOW(),
		#{lastUpdateUserId,jdbcType=VARCHAR},
		NOW())
		<selectKey resultType="long" keyProperty="id" order="AFTER">
			SELECT LAST_INSERT_ID() AS ID
		</selectKey>
	</insert>

	<update id="update" parameterType="org.smartas.attachment.Attachment">
		UPDATE tpl_file_t SET
		batch_no = #{batchNo,jdbcType=VARCHAR},
		file_path = #{filePath,jdbcType=VARCHAR},
		file_name = #{fileName,jdbcType=VARCHAR},
		file_uid = #{fileUid,jdbcType=VARCHAR},
		revision = #{revision,jdbcType=NUMERIC},
		last_update_user_id = #{lastUpdateUserId,jdbcType=VARCHAR},
		last_update_date = #{lastUpdateDate,jdbcType=DATE}
		WHERE ID = #{id,jdbcType=NUMERIC}
	</update>


	<select id="deleteById">
		DELETE FROM tpl_file_t
		WHERE ID = #{id}
	</select>
	
	<!-- //////////////////////////////////////////////////////////// -->

	<!-- 
	<select id="findNavbarMenus" resultType="org.smartas.attachment.Attachment">
		SELECT
		T.*
		FROM oa_orgnazition_t T
		WHERE T.org_code = ''
	</select>
	-->
	
	<select id="getByName" resultType="int">
		SELECT
		count(1)
		FROM tpl_file_t T
		WHERE T.file_name = #{fileName}
	</select>
	
</mapper>

